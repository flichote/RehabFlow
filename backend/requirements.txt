# === RehabFlow 后端依赖（M1） ===
# 版本策略见 docs/tech-stack.md §2（context7 已核实最新）
# SQLite 开发起步 → PG16 生产切换只需改 DATABASE_URL，驱动已备

fastapi[standard]>=0.110
uvicorn[standard]>=0.29
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
pydantic>=2.7,<3.0
pydantic-settings>=2.0
alembic>=1.13
asyncpg>=0.29
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# bcrypt 固定 4.0.1：5.x 移除 __about__ 导致 passlib 1.7.4 检测崩溃（AttributeError）
bcrypt==4.0.1
apscheduler>=3.10
httpx>=0.27

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
